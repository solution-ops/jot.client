import { StrictMode } from 'react';
import ReactDOM from 'react-dom';

// local dependencies
import './index.css';
import App from './App';

ReactDOM.render(
  <StrictMode>
    <App />
  </StrictMode>,
  document.getElementById('root'),
);
